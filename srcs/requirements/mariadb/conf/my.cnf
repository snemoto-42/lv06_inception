[mysql]
# コマンドは下記ユーザーで実施
user = mysql

[mysqld]
# コマンドは下記ユーザーで実施
user = mysql

# データベースファイルを格納するディレクトリ
datadir = /var/lib/mysql

# ソケットファイルの場所
socket = /run/mysqld/mysqld.sock

# 接続を受け付けるホスト
bind-address = 0.0.0.0

# サーバーのポート番号
port = 3306

# Windows や macOS などのファイルシステムではファイルシステムが大文字と小文字を区別しない
# 1に設定することで、大文字と小文字を区別しないようにすることが一般的
lower_case_table_names = 1
